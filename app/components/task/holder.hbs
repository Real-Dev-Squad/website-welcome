<div class="task-card">
  <Task-Details @task={{@task}} />
  {{#if (eq @task.status 'IN_PROGRESS')}}
    <div class="progress-bar-container">
      <div class="progress-bar">
        <input
          id="input"
          type="range"
          name="percentCompleted"
          value="{{@task.percentCompleted}}"
          min="0" max="100" step="25"
          {{on "change" this.onPercentageChange}}
          {{on "change" (fn @onTaskUpdate @task.id)}}
        />  
      </div>
    </div>
  {{/if}}
  <div>
    {{#if (eq @userSelectedTask.key 'all')}}
      <div class="current-task-status"><strong>Current task status:</strong> {{@task.status}}</div>
    {{/if}}
    <label for="task-update">Change task status:</label>
    <select id="task-update" 
    {{on "change" this.onStatusChange}}
    {{on "change" (fn @onTaskUpdate @task.id)}}
    >
      {{#each this.availableTaskStatuses as |taskStatus|}}
        {{#if (not-eq taskStatus.key 'all')}}
          <option value={{taskStatus.key}}>
            {{taskStatus.label}}
          </option>
        {{/if}}
      {{/each}}
    </select>

     {{#if @isLoading}}
       <div class="loader-container">
         <Spinner />
       </div>
     {{/if}}
  </div>
</div>
